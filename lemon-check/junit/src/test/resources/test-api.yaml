openapi: 3.0.3
info:
  title: Test API
  description: Test API for LemonCheck JUnit tests
  version: 1.0.0
servers:
  - url: http://localhost:8080/api
paths:
  /test:
    get:
      operationId: getTest
      summary: Get test endpoint
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
